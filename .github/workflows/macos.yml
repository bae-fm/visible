name: macOS

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  build:
    name: Build macOS app
    runs-on: macos-26

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Rust environment
      uses: ./.github/actions/setup-rust

    - name: Install xcodegen
      run: brew install xcodegen

    - name: Build bridge (native arm64, generate Swift bindings, package xcframework)
      run: ./visible-bridge/build-macos.sh

    - name: Generate Xcode project
      run: cd visible-macos/visible && xcodegen generate

    - name: Build + test macOS app (unsigned)
      # `test` on the visibleTests scheme builds the host app, then runs the
      # host-run unit tests for the shared Swift model logic — which is the iOS
      # app's logic too (the model sources are shared), so iOS needs no separate
      # test run. A test failure fails the job.
      run: |
        xcodebuild \
          -project visible-macos/visible/visible.xcodeproj \
          -scheme visibleTests \
          -configuration Debug \
          -destination 'platform=macOS' \
          CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO \
          test
