<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Cloud sync uploads encrypted blobs to an S3 cloud home. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".VisibleApp"
        android:label="visible"
        android:supportsRtl="true"
        android:theme="@style/Theme.Visible">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|smallestScreenSize|keyboardHidden|locale|layoutDirection|fontScale|uiMode|density"
            android:theme="@style/Theme.Visible">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Grants the camera app a temporary content uri to write the captured
             photo into our private files dir, which TakePicture reads back. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="fm.bae.visible.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
